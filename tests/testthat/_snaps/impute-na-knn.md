# impute_na_knn rejects k when too few donors are available

    Code
      impute_na_knn(dat, k = 5, verbose = FALSE)
    Condition
      Error in `impute_na_knn()`:
      ! Not enough observed donors for `k = 5`.
      x Insufficient donors: value (1).
      i Reduce `k` or provide more observed values.

