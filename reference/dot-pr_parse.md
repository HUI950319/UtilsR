# Parse diverse inputs into a named list of named numeric vectors

Parse diverse inputs into a named list of named numeric vectors

## Usage

``` r
.pr_parse(data, group_col, name_col, value_col, groups)
```
