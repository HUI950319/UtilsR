% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotScatter1.R
\name{PlotScatter1}
\alias{PlotScatter1}
\title{Grouped Scatter Plot with Marginal Boxplots}
\usage{
PlotScatter1(
  data,
  x,
  y,
  group,
  group_levels = NULL,
  colors = NULL,
  shapes = NULL,
  point_size = 4,
  point_alpha = 0.8,
  show_ellipse = TRUE,
  ellipse_level = 0.95,
  show_refline = TRUE,
  show_regression = FALSE,
  reg_method = "lm",
  show_cor = FALSE,
  cor_method = "pearson",
  annot_text = NULL,
  annot_size = 4,
  xlim = NULL,
  ylim = NULL,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  marginal_type = c("boxplot", "violin", "violin_box"),
  violin_scale = "width",
  box_jitter = TRUE,
  layout_ratio = c(1, 4),
  legend_pos = c(0.85, 0.15),
  legend_theme = NULL,
  theme_use = NULL,
  raster = FALSE,
  raster_method = c("scattermore", "ggrastr"),
  raster.dpi = 512,
  filename = NULL,
  width = 10,
  height = 8,
  dpi = 300
)
}
\arguments{
\item{data}{A data.frame.}

\item{x}{Column name for the X axis.}

\item{y}{Column name for the Y axis.}

\item{group}{Column name for the grouping variable.}

\item{group_levels}{Group order. NULL = data order.}

\item{colors}{Colour vector for groups. NULL = auto palette.}

\item{shapes}{Shape vector for groups. NULL = auto.}

\item{point_size}{Point size. Default 4.}

\item{point_alpha}{Point transparency. Default 0.8.}

\item{show_ellipse}{Whether to draw group ellipses. Default TRUE.}

\item{ellipse_level}{Ellipse confidence level. Default 0.95.}

\item{show_refline}{Whether to show x=0, y=0 reference lines. Default TRUE.}

\item{show_regression}{Whether to show a regression line. Default FALSE.}

\item{reg_method}{Regression method: "lm" or "loess". Default "lm".}

\item{show_cor}{Whether to display correlation coefficient. Default FALSE.}

\item{cor_method}{Correlation method: "pearson" or "spearman". Default "pearson".}

\item{annot_text}{Custom annotation text (e.g. PERMANOVA result). NULL = none.}

\item{annot_size}{Annotation text size. Default 4.}

\item{xlim}{Custom X axis limits, e.g. \code{c(0, 1)}. NULL = auto (with
default 5\% expansion). When set, expansion is suppressed (\code{expand = FALSE}).}

\item{ylim}{Custom Y axis limits, e.g. \code{c(0, 1)}. NULL = auto.}

\item{xlab}{X axis label. NULL = column name.}

\item{ylab}{Y axis label. NULL = column name.}

\item{title}{Plot title. Default NULL.}

\item{marginal_type}{Marginal plot type: "boxplot", "violin", or "violin_box".}

\item{violin_scale}{Scale method for violin geom: \code{"width"} (default,
normalises max-width so both marginals look visually consistent),
\code{"area"}, or \code{"count"}.}

\item{box_jitter}{Whether to add jitter points in marginal plots. Default TRUE.}

\item{layout_ratio}{Size ratio c(marginal, main). Default c(1, 4).}

\item{legend_pos}{Legend position. Default c(0.85, 0.15).}

\item{legend_theme}{Legend theme object (e.g. \code{theme_legend1()}). Default NULL.}

\item{theme_use}{Theme for the main scatter panel. Default NULL (built-in theme).}

\item{raster}{Logical. Rasterise scatter points to reduce file size.
\code{FALSE} = vector (default). \code{TRUE} = raster.}

\item{raster_method}{Character. Rasterisation backend when \code{raster = TRUE}:
\code{"scattermore"} (default, fast, requires \pkg{scattermore}) or
\code{"ggrastr"} (faithful colours, requires \pkg{ggrastr}).}

\item{raster.dpi}{Integer. Raster resolution in pixels (for scattermore) or
DPI (for ggrastr). Default \code{512}.}

\item{filename}{Output file path. NULL = no save.}

\item{width}{Output width in inches. Default 10.}

\item{height}{Output height in inches. Default 8.}

\item{dpi}{Output resolution. Default 300.}
}
\value{
A patchwork object (combined plot).
}
\description{
Single-group scatter plot with top and right marginal boxplots (with jitter)
and optional annotation panel (PERMANOVA / correlation). Suitable for
PCoA/NMDS ordination or gene correlation visualisation.
}
\details{
For dual-group scatter with separate marginal panels, see [PlotScatter2()].
For paired scatter with rotated histogram inset, see [PlotScatter3()].
}
\examples{
library(ggplot2)

# Simulated PCoA data
set.seed(42)
df <- data.frame(
  PC1   = c(rnorm(30, -1), rnorm(30, 1)),
  PC2   = c(rnorm(30, 0.5), rnorm(30, -0.5)),
  Group = rep(c("Control", "Treatment"), each = 30)
)

# Basic usage
PlotScatter1(df, x = "PC1", y = "PC2", group = "Group")

# With correlation and violin marginals
PlotScatter1(df, x = "PC1", y = "PC2", group = "Group",
             show_cor = TRUE, marginal_type = "violin")

# Custom annotation (e.g. PERMANOVA)
PlotScatter1(df, x = "PC1", y = "PC2", group = "Group",
             annot_text = "PERMANOVA\nR2 = 0.15, p = 0.001")

}
\seealso{
Other scatter plots: 
\code{\link{PlotScatter2}()},
\code{\link{PlotScatter3}()}
}
\concept{scatter plots}
