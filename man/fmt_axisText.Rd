% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_plot.R
\name{fmt_axisText}
\alias{fmt_axisText}
\title{Format axis text rotation and style}
\usage{
fmt_axisText(
  plot,
  x = NULL,
  y = NULL,
  x_hjust = NULL,
  x_vjust = NULL,
  y_hjust = NULL,
  y_vjust = NULL,
  size = NULL,
  color = NULL,
  face = NULL,
  ...
)
}
\arguments{
\item{plot}{A ggplot, patchwork, or list of ggplot objects.}

\item{x}{Numeric. Rotation angle (degrees) for X-axis text.
Common values: \code{45} (diagonal), \code{90} (vertical).
Default \code{NULL} (no change).}

\item{y}{Numeric. Rotation angle (degrees) for Y-axis text.
Default \code{NULL} (no change).}

\item{x_hjust}{Numeric. Horizontal justification for X-axis text.
Default \code{NULL} (auto: 1 when \code{x > 0}, 0 when \code{x < 0},
0.5 when \code{x = 0}).}

\item{x_vjust}{Numeric. Vertical justification for X-axis text.
Default \code{NULL} (auto: 0.5 when \code{abs(x) >= 90}, 1 otherwise).}

\item{y_hjust}{Numeric. Horizontal justification for Y-axis text.
Default \code{NULL} (auto).}

\item{y_vjust}{Numeric. Vertical justification for Y-axis text.
Default \code{NULL} (auto).}

\item{size}{Numeric. Text size for both axes. Default \code{NULL}
(no change).}

\item{color}{Character. Text color for both axes. Default \code{NULL}
(no change).}

\item{face}{Character. Font face (\code{"plain"}, \code{"bold"},
\code{"italic"}, \code{"bold.italic"}). Default \code{NULL}
(no change).}

\item{...}{Additional arguments passed to [ggplot2::theme()].}
}
\value{
Same type as input.
}
\description{
Rotate and style axis tick labels for X and/or Y axes. Inspired by
Seurat's \code{RotatedAxis()} but with independent control over both
axes, rotation angle, and text appearance.
}
\examples{
library(ggplot2)
p <- ggplot(iris, aes(Species, Sepal.Length)) + geom_boxplot()

# Rotate X-axis 45 degrees (like Seurat::RotatedAxis())
fmt_axisText(p, x = 45)

# Rotate X-axis 90 degrees (vertical)
fmt_axisText(p, x = 90)

# Rotate both axes
fmt_axisText(p, x = 45, y = -30)

# With custom size and bold
fmt_axisText(p, x = 45, size = 10, face = "bold")

}
\seealso{
Other plot formatting: 
\code{\link{fmt_axis}()},
\code{\link{fmt_axisTile}()},
\code{\link{fmt_bg}()},
\code{\link{fmt_boxplot}()},
\code{\link{fmt_com}()},
\code{\link{fmt_expand}()},
\code{\link{fmt_his}()},
\code{\link{fmt_legend}()},
\code{\link{fmt_panel}()},
\code{\link{fmt_plot}()},
\code{\link{fmt_point}()},
\code{\link{fmt_ref}()},
\code{\link{fmt_scale}()},
\code{\link{fmt_strip}()},
\code{\link{fmt_tag}()},
\code{\link{fmt_text}()}
}
\concept{plot formatting}
