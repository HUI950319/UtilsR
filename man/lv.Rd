% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lv.R
\name{lv}
\alias{lv}
\alias{lv.default}
\alias{lv.data.frame}
\alias{lv.Seurat}
\title{Variable Inspection Generic Function}
\usage{
lv(data, ...)

\method{lv}{default}(data, ..., pattern = NULL, group = NULL, count = NULL)

\method{lv}{data.frame}(data, ..., pattern = NULL, group = NULL, count = NULL)

\method{lv}{Seurat}(
  data,
  ...,
  pattern = NULL,
  group = NULL,
  count = NULL,
  add_reductions = FALSE,
  add_cell_stats = TRUE
)
}
\arguments{
\item{data}{A data object: data.frame or Seurat object.}

\item{...}{Variable selection using \code{dplyr::select} syntax.}

\item{pattern}{Optional regex pattern for matching variable names.}

\item{group}{Optional grouping variable. Supports NSE (bare name) or
character string. For Seurat objects, set to \code{TRUE} to use current
Ident.}

\item{count}{Optional variable(s) for cross-tabulation. Accepts a single
variable name (NSE or string), multiple variable names via
\code{c(var1, var2)} (NSE) or character vector
\code{c("var1", "var2")}. Independent of \code{group}.}

\item{add_reductions}{Logical. Whether to add reduction coordinates. Default
\code{FALSE}.}

\item{add_cell_stats}{Logical. Whether to show cell statistics. Default
\code{TRUE}.}
}
\value{
Invisibly returns the input data.
}
\description{
Inspect variables in a data.frame or Seurat object, including frequency
counts, unique values, missing values and special values (e.g. "unknown",
"blank", "n/a"). Supports grouped summaries and multi-variable
cross-tabulation.
}
\examples{
\dontrun{
# data.frame basic usage
lv(mtcars)
lv(mtcars, mpg, cyl, hp)
lv(mtcars, pattern = "mpg")

# Grouped view (supports NSE)
lv(mtcars, mpg, hp, wt, group = cyl)
lv(mtcars, mpg, hp, wt, group = "cyl")

# Cross-tabulation
lv(mtcars, count = cyl)
lv(mtcars, count = c(cyl, gear))
lv(mtcars, count = c("cyl", "gear"))

# Seurat object
lv(seurat_obj)
lv(seurat_obj, group = TRUE)
lv(seurat_obj, count = c(seurat_clusters, Phase))
}

}
