% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotRank.R
\name{PlotRank}
\alias{PlotRank}
\title{Rank Scatter Plot}
\usage{
PlotRank(
  data,
  group_col = "cell_type",
  name_col = "gene",
  value_col = "importance",
  groups = NULL,
  group_levels = NULL,
  top_n = 5L,
  max_show = 200L,
  highlight_color = "#007D9B",
  base_color = "#BECEE3",
  label_size = 4,
  point_size = 3,
  title = NULL,
  ylab = "Importance",
  base_size = 12,
  ncol = 4L,
  clean_names = TRUE,
  return_type = c("plot", "data", "both"),
  filename = NULL,
  width = 12,
  height = 10,
  dpi = 300
)
}
\arguments{
\item{data}{Input data in one of the following formats:
\describe{
  \item{data.frame}{Must contain columns specified by \code{name_col}
    and \code{value_col}.  If \code{group_col} exists, a faceted
    multi-panel plot is produced.}
  \item{Named numeric vector}{Values are scores; names are feature
    labels.  Produces a single-panel plot.}
  \item{Matrix}{Rows = features, columns = groups (e.g. cell types).
    Use \code{groups} to select specific columns.}
}}

\item{group_col}{Column name for the grouping variable (e.g. cell type).
Default \code{"cell_type"}.  Ignored for vector / matrix input.}

\item{name_col}{Column name for the feature names.
Default \code{"gene"}.  Ignored for vector / matrix input.}

\item{value_col}{Column name for the numeric scores.
Default \code{"importance"}.  Ignored for vector / matrix input.}

\item{groups}{Character vector of groups to plot (for matrix or
data.frame input).  \code{NULL} (default) = all groups.}

\item{group_levels}{Character vector specifying display order.
\code{NULL} = data appearance order or factor levels.}

\item{top_n}{Integer. Number of top-ranked features to highlight
per group. Default \code{5}.}

\item{max_show}{Integer. Maximum features to display per panel.
Default \code{200}.}

\item{highlight_color}{Colour for top-ranked points.
Default \code{"#007D9B"}.}

\item{base_color}{Colour for remaining points.
Default \code{"#BECEE3"}.}

\item{label_size}{Numeric. Text label size. Default \code{4}.}

\item{point_size}{Numeric. Point size. Default \code{3}.}

\item{title}{Character. Plot title. \code{NULL} = auto.}

\item{ylab}{Character. Y-axis label. Default \code{"Importance"}.}

\item{base_size}{Numeric. Base font size. Default \code{12}.}

\item{ncol}{Integer. Number of columns in faceted layout. Default \code{4}.}

\item{clean_names}{Logical. Strip common prefixes (\code{HALLMARK_},
\code{KEGG_}, etc.) and replace underscores with spaces.
Default \code{TRUE}.}

\item{return_type}{What to return: \code{"plot"} (default),
\code{"data"} (the ranked data.frame), or \code{"both"}.}

\item{filename}{Output file path. \code{NULL} = no save.}

\item{width}{Output width in inches. Default \code{12}.}

\item{height}{Output height in inches. Default \code{10}.}

\item{dpi}{Output resolution. Default \code{300}.}
}
\value{
Depends on \code{return_type}:
  \describe{
    \item{"plot"}{A \code{ggplot} object (default).}
    \item{"data"}{A data.frame with columns: Group, Rank, Score, Label, IsTop.}
    \item{"both"}{A list with elements \code{plot} and \code{data}.}
  }
}
\description{
Ranks features (genes, pathways, regulons, etc.) by their scores and
highlights the top-ranked ones with coloured points and text labels.
Designed for DNN model interpretation outputs (Integrated Gradients
importance scores) but works with any named score data.
}
\examples{
\dontrun{
# 1) DNN per-class gene importance (data.frame, faceted)
PlotRank(pred$imp_per_class, top_n = 10, ncol = 4)

# 2) Single group from per-class data
PlotRank(pred$imp_per_class, groups = "Parathyroid cells", top_n = 15)

# 3) Global gene importance (data.frame, single panel)
PlotRank(pred$imp_global, name_col = "gene", value_col = "importance",
         top_n = 20, ylab = "Global IG Importance")

# 4) Pathway scores matrix
PlotRank(pred$pathway_scores, top_n = 10, ylab = "Pathway Score")

# 5) Named numeric vector
scores <- setNames(runif(100), paste0("Gene", 1:100))
PlotRank(scores, top_n = 10)

# 6) Save to file
PlotRank(pred$imp_per_class, top_n = 10,
         filename = "rank_scatter.pdf", width = 16, height = 12)
}

}
