% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plt_sankey.R
\name{plt_sankey}
\alias{plt_sankey}
\title{Sankey Diagram for Categorical Variables}
\usage{
plt_sankey(
  data,
  vars,
  palette = NULL,
  reverse_levels = TRUE,
  show_text = c("all", "n", "pct", "name"),
  width = 0.4,
  space = NULL,
  label_size = 3,
  label_hjust = 0.5,
  alpha = 0.6,
  base_size = 14
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of categorical variable names (>= 2).
Variables are displayed left-to-right in the given order.}

\item{palette}{Colour palette name from \code{pal_get()}, or a character
vector of colours. Default \code{NULL} auto-generates colours per variable
using sequential HCL palettes.}

\item{reverse_levels}{Logical, reverse factor levels for display.
Default \code{TRUE}.}

\item{show_text}{Character. Controls node label content:
\itemize{
  \item \code{"all"} (default) — name + count + percentage, e.g.
    \code{"A (10, 30.3\%)"}.
  \item \code{"n"} — name + count, e.g. \code{"A (10)"}.
  \item \code{"pct"} — name + percentage, e.g. \code{"A (30.3\%)"}.
  \item \code{"name"} — name only, e.g. \code{"A"}.
}}

\item{width}{Sankey node width. Default 0.4.}

\item{space}{Numeric. Gap between nodes within each column. Default
\code{NULL} (ggsankey auto-calculates). Set \code{0} for no gaps
(consistent total height across columns).}

\item{label_size}{Label text size. Default 3.}

\item{label_hjust}{Label horizontal justification. Default 0.5.}

\item{alpha}{Flow transparency. Default 0.6.}

\item{base_size}{Base font size. Default 14.}
}
\value{
A ggplot object.
}
\description{
Visualise the flow/proportion changes across multiple categorical variables
using a Sankey (alluvial) diagram. Each node label shows the level name,
count, and percentage.
}
\note{
Requires the \pkg{ggsankey} package
  (\code{pak::pak("davidsjoberg/ggsankey")}).
}
\examples{
df <- data.frame(
  sex = factor(sample(c("M","F"), 200, TRUE)),
  stage = factor(sample(c("I","II","III"), 200, TRUE)),
  grade = factor(sample(c("Low","High"), 200, TRUE))
)

# Basic sankey
plt_sankey(df, vars = c("sex", "stage", "grade"))

# Two variables
plt_sankey(df, vars = c("sex", "stage"))

# Custom palette
plt_sankey(df, vars = c("sex", "stage"), palette = "Paired")

# Without counts in labels
plt_sankey(df, vars = c("sex", "stage", "grade"), show_n = FALSE)

# Adjust appearance
plt_sankey(df, vars = c("sex", "stage"),
           width = 0.3, label_size = 4, alpha = 0.4)

}
\seealso{
Other plot: 
\code{\link{PlotButterfly}()},
\code{\link{PlotButterfly2}()},
\code{\link{PlotRankCor}()},
\code{\link{plt_cat}()},
\code{\link{plt_cohen}()},
\code{\link{plt_con}()},
\code{\link{plt_dist}()},
\code{\link{plt_radar}()},
\code{\link{plt_upset}()}
}
\concept{plot}
