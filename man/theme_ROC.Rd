% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_ROC}
\alias{theme_ROC}
\title{ROC / Calibration Plot Theme}
\usage{
theme_ROC(
  base_size = 11,
  axis_text_size = 13,
  axis_title_size = 14,
  title_size = 16,
  subtitle_size = 9,
  strip_size = 12,
  legend_text_size = 9,
  legend_title_size = 8,
  legend.position = c(0.8, 0.3),
  axis_linewidth = 0.8,
  plot_margin = ggplot2::margin(10, 10, 10, 10)
)
}
\arguments{
\item{base_size}{Numeric. Base font size (default 11).}

\item{axis_text_size}{Numeric. Axis tick-label size (default 13).}

\item{axis_title_size}{Numeric. Axis title size (default 14).}

\item{title_size}{Numeric. Plot title size (default 16).}

\item{subtitle_size}{Numeric. Subtitle size (default 9).}

\item{strip_size}{Numeric. Facet strip text size (default 12).}

\item{legend_text_size}{Numeric. Legend text size (default 9).}

\item{legend_title_size}{Numeric. Legend title size (default 8).}

\item{legend.position}{Legend position. Default \code{c(0.8, 0.3)} (inside
panel). Use \code{"right"}, \code{"bottom"}, etc. for outside placement.}

\item{axis_linewidth}{Numeric. Axis line and tick width (default 0.8).}

\item{plot_margin}{Margin around the plot in pt. Default
\code{margin(10, 10, 10, 10)}.}
}
\value{
A ggplot2 theme object.
}
\description{
A clean ggplot2 theme designed for ROC curves, calibration plots, and similar
diagnostic plots. Based on \code{theme_classic} with bold axis text, centred
titles, and a compact legend positioned inside the panel.
}
\examples{
library(ggplot2)

# Basic ROC-style plot
set.seed(1)
df <- data.frame(FPR = seq(0, 1, length.out = 100),
                 TPR = sort(runif(100))^0.5)
ggplot(df, aes(FPR, TPR)) +
  geom_line(color = "steelblue", linewidth = 1) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed") +
  coord_equal() +
  theme_ROC()

# Customise legend position
ggplot(df, aes(FPR, TPR)) +
  geom_line() +
  theme_ROC(legend.position = "right")

}
\seealso{
Other ggplot2 themes: 
\code{\link{leg1}()},
\code{\link{leg2}()},
\code{\link{theme_alluvia}()},
\code{\link{theme_blank}()},
\code{\link{theme_km}},
\code{\link{theme_legend}()},
\code{\link{theme_legend1}()},
\code{\link{theme_my}()},
\code{\link{theme_rcs}},
\code{\link{theme_sc}()},
\code{\link{theme_scatter}}
}
\concept{ggplot2 themes}
