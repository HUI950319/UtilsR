% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_na_knn.R
\name{impute_na_knn}
\alias{impute_na_knn}
\title{Impute Missing Values with Weighted K-Nearest Neighbours}
\usage{
impute_na_knn(data, k = 5, verbose = TRUE)
}
\arguments{
\item{data}{A data frame or tibble. Other data-frame subclasses are processed
as plain data frames.}

\item{k}{A positive integer giving the number of nearest neighbours. Defaults
to \code{5}. It must not exceed the number of observed donors in any
variable being imputed.}

\item{verbose}{Logical. If \code{TRUE}, report the variables being imputed.}
}
\value{
A data frame with missing values imputed where possible. A tibble is
  returned when \code{data} is a tibble.
}
\description{
Impute missing values in mixed-type data with
\code{\link[VIM]{kNN}}. Labelled variables are temporarily converted to
factors for distance calculation, then restored with their original storage
type, variable label, value labels, and other attributes.
}
\examples{
dat <- data.frame(
  age = c(30, 40, NA, 60),
  group = factor(c("A", "B", "B", "A")),
  marker = c(1, 5, 5, 1)
)

if (requireNamespace("VIM", quietly = TRUE)) {
  impute_na_knn(dat, k = 1, verbose = FALSE)
}

}
\seealso{
\code{\link{check_na}()}

Other inspect:
\code{\link{check_na}()},
\code{\link{check_size}()},
\code{\link{check_system}()},
\code{\link{count_packages_in_libpaths}()},
\code{\link{lv}()}
}
\concept{inspect}
