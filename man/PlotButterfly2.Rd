% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotButterfly2.R
\name{PlotButterfly2}
\alias{PlotButterfly2}
\title{Butterfly Plot from Long-Format Data}
\usage{
PlotButterfly2(
  data,
  stat.by = NULL,
  value.by = NULL,
  group.by = NULL,
  fill.by = NULL,
  type = c("violin_box", "violin", "box", "beeswarm", "beeswarm_quasirandom"),
  levels = NULL,
  palette = NULL,
  alpha = 0.85,
  box.width = 0.15,
  add_point = FALSE,
  pt.size = 0.8,
  pt.alpha = 0.5,
  violin.scale = "width",
  dodge.width = 0.8,
  color_by = c("fixed", "logFC"),
  fixed_color = "#f33131",
  gradient_colors = c("#2166AC", "white", "#B2182B"),
  cex = 0.3,
  ref.line = NULL,
  ref.color = "#bf1a2c",
  text.colors = NULL,
  left.title = NULL,
  right.title = NULL,
  left.title.color = "#f04625",
  right.title.color = "#00a8ee",
  title = NULL,
  xlab = NULL,
  base_size = 14,
  legend.position = "bottom",
  legend_theme = NULL,
  width = 12,
  height = 8,
  filename = NULL
)
}
\arguments{
\item{data}{Data frame in long format. Each row is one observation.}

\item{stat.by}{Character. Column for the y-axis categories.}

\item{value.by}{Character. Column for the continuous x-axis value.}

\item{group.by}{Character. Column defining the left/right panels
(must have exactly 2 unique levels).}

\item{fill.by}{Character or \code{NULL}. Column for dodge grouping.
Default \code{NULL}.}

\item{type}{Chart type: \code{"violin_box"} (default), \code{"violin"},
\code{"box"}, \code{"beeswarm"}, or \code{"beeswarm_quasirandom"}.}

\item{levels}{Display order for y-axis categories. One of:
\code{NULL} (default), \code{"up"} (ascending by mean),
\code{"down"} (descending), or a character vector.}

\item{palette}{Colour palette for \code{fill.by} levels. Default
\code{NULL} uses \code{pal_lancet}.}

\item{alpha}{Numeric 0--1. Fill/point transparency. Default 0.85.}

\item{box.width}{Numeric. Boxplot width. Default 0.15.}

\item{add_point}{Logical. Overlay jittered points (violin/box modes)?
Default \code{FALSE}.}

\item{pt.size}{Numeric. Point size. Default 0.8.}

\item{pt.alpha}{Numeric. Point alpha. Default 0.5.}

\item{violin.scale}{Character. How violin widths are scaled:
\code{"width"} (default), \code{"area"}, or \code{"count"}.}

\item{dodge.width}{Numeric. Dodge width. Default 0.8.}

\item{color_by}{Character. Beeswarm colouring mode when
\code{fill.by = NULL}: \code{"fixed"} (default, single colour) or
\code{"logFC"} (gradient by \code{value.by}).}

\item{fixed_color}{Character. Point colour for \code{color_by = "fixed"}.
Default \code{"#f33131"}.}

\item{gradient_colors}{Character vector of length 3 (low, mid, high)
for \code{color_by = "logFC"}. Default
\code{c("#2166AC", "white", "#B2182B")}.}

\item{cex}{Numeric. Beeswarm point spacing (passed to
\code{geom_beeswarm}). Default 0.3.}

\item{ref.line}{Numeric or \code{NULL}. Reference line. Default
\code{NULL}.}

\item{ref.color}{Character. Reference line colour. Default
\code{"#bf1a2c"}.}

\item{text.colors}{Character vector. Per-variable y-axis colours.
Default \code{NULL}.}

\item{left.title, right.title}{Character. Panel titles. Default
\code{NULL} (auto from \code{group.by} levels).}

\item{left.title.color, right.title.color}{Character. Title colours.
Default \code{"#f04625"} / \code{"#00a8ee"}.}

\item{title}{Character. Overall title. Default \code{NULL}.}

\item{xlab}{Character. X-axis label. Default \code{NULL}.}

\item{base_size}{Numeric. Base font size. Default 14.}

\item{legend.position}{Legend position. Default \code{"bottom"}.}

\item{legend_theme}{A ggplot2 theme object. Default \code{NULL}.}

\item{width, height}{Numeric. Output size in inches. Default 12 / 8.}

\item{filename}{Character or \code{NULL}. Save path. Default
\code{NULL}.}
}
\value{
A \code{patchwork} object.
}
\description{
Create a butterfly (back-to-back) plot from a single long-format data frame.
The \code{group.by} column (exactly 2 levels) defines the left/right panels.
An optional \code{fill.by} column adds a dodge dimension.
}
\details{
Five plot types are available:
\itemize{
  \item \code{"violin_box"}: violin + boxplot overlay (default).
  \item \code{"violin"}: violin only.
  \item \code{"box"}: boxplot only.
  \item \code{"beeswarm"}: deterministic beeswarm points
    (\code{\link[ggbeeswarm]{geom_beeswarm}}).
  \item \code{"beeswarm_quasirandom"}: quasi-random beeswarm points
    (\code{\link[ggbeeswarm]{geom_quasirandom}}).
}
}
\examples{
set.seed(1)
df <- data.frame(
  celltype   = rep(LETTERS[1:5], each = 40, times = 4),
  logFC      = rnorm(800),
  method     = rep(rep(c("scMMR", "miloR"), each = 200), 2),
  comparison = rep(c("PHPT", "SHPT"), each = 400)
)

# Violin + boxplot (default)
PlotButterfly2(df, stat.by = "celltype", value.by = "logFC",
               group.by = "method", fill.by = "comparison",
               ref.line = 0)

# Beeswarm with dodge
PlotButterfly2(df, stat.by = "celltype", value.by = "logFC",
               group.by = "method", fill.by = "comparison",
               type = "beeswarm_quasirandom", ref.line = 0)

# Beeswarm with gradient colour
PlotButterfly2(df[df$comparison == "PHPT", ],
               stat.by = "celltype", value.by = "logFC",
               group.by = "method", type = "beeswarm",
               color_by = "logFC", ref.line = 0)

}
\seealso{
\code{\link{PlotButterfly}}

Other plot: 
\code{\link{PlotButterfly}()},
\code{\link{PlotRankCor}()},
\code{\link{plt_cat}()},
\code{\link{plt_cohen}()},
\code{\link{plt_con}()},
\code{\link{plt_dist}()},
\code{\link{plt_radar}()},
\code{\link{plt_sankey}()},
\code{\link{plt_upset}()}
}
\concept{plot}
