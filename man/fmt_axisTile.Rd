% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_plot.R
\name{fmt_axisTile}
\alias{fmt_axisTile}
\title{Add colored tiles between axis and labels}
\usage{
fmt_axisTile(
  plot,
  colors,
  mode = c("tile", "text"),
  axis = c("x", "y"),
  tile_height = 0.06,
  tile_width = 0.06,
  tile_border = "white",
  tile_border_width = 0.2,
  text_size = 9,
  text_face = "plain",
  text_angle = NULL,
  text_color = "black",
  show_text = TRUE
)
}
\arguments{
\item{plot}{A ggplot, patchwork, or list of ggplot objects.}

\item{colors}{Named character vector of colors, where names match the
discrete axis levels (e.g. \code{c(setosa = "red", virginica = "blue")}).
Required.}

\item{mode}{\code{"tile"} (default) or \code{"text"}.}

\item{axis}{Which axis to apply to: \code{"x"} (default) or \code{"y"}.}

\item{tile_height}{Numeric. Relative height of the tile strip when
\code{axis = "x"}. Default 0.06.}

\item{tile_width}{Numeric. Relative width of the tile strip when
\code{axis = "y"}. Default 0.06.}

\item{tile_border}{Color of tile borders. Default \code{"white"}.}

\item{tile_border_width}{Line width of tile borders. Default 0.2.}

\item{text_size}{Size of axis text labels below/beside tiles. Default 9.}

\item{text_face}{Font face of axis text labels. Default \code{"plain"}.}

\item{text_angle}{Rotation angle for axis text labels. Default \code{45}
for x-axis, \code{0} for y-axis.}

\item{text_color}{Color of axis text labels. Default \code{"black"}.}

\item{show_text}{Logical. Show text labels below/beside tiles?
Default \code{TRUE}. Set \code{FALSE} for color-only tiles.}
}
\value{
A patchwork object (when \code{mode = "tile"}) or same type as
  input (when \code{mode = "text"}).
}
\description{
Insert a strip of colored tiles between the plot area and the axis text
labels. Two modes are available:
\itemize{
  \item \code{"tile"} (default) — insert a \code{geom_tile} color strip
    between the axis line and text labels via patchwork. Labels appear
    below (x-axis) or beside (y-axis) the tiles.
  \item \code{"text"} — color the axis label text directly (no background
    tile), lightweight but uses unofficial vectorized \code{element_text}.
}
}
\examples{
library(ggplot2)
cols <- c(setosa = "#E64B35", versicolor = "#4DBBD5", virginica = "#00A087")
p <- ggplot(iris, aes(Species, Sepal.Length)) + geom_boxplot()

# Tile mode: color strip between axis and rotated labels
fmt_axisTile(p, colors = cols)

# Tile mode: no text labels, color-only strip
fmt_axisTile(p, colors = cols, show_text = FALSE)

# Text mode: colored axis labels (no tiles)
fmt_axisTile(p, colors = cols, mode = "text")

# Y-axis tiles
p2 <- ggplot(iris, aes(Sepal.Length, Species)) + geom_boxplot()
fmt_axisTile(p2, colors = cols, axis = "y")

}
\seealso{
Other plot formatting: 
\code{\link{fmt_axis}()},
\code{\link{fmt_axisText}()},
\code{\link{fmt_bg}()},
\code{\link{fmt_boxplot}()},
\code{\link{fmt_com}()},
\code{\link{fmt_expand}()},
\code{\link{fmt_his}()},
\code{\link{fmt_legend}()},
\code{\link{fmt_panel}()},
\code{\link{fmt_plot}()},
\code{\link{fmt_point}()},
\code{\link{fmt_ref}()},
\code{\link{fmt_scale}()},
\code{\link{fmt_strip}()},
\code{\link{fmt_tag}()},
\code{\link{fmt_text}()}
}
\concept{plot formatting}
