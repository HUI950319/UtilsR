% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plt_cohen.R
\name{plt_cohen}
\alias{plt_cohen}
\title{Plot Cohen's d Effect Sizes}
\usage{
plt_cohen(
  res,
  ref_group = NULL,
  palette = NULL,
  threshold = c(-0.2, 0.2),
  add_line = TRUE,
  facet = FALSE,
  base_size = 14
)
}
\arguments{
\item{res}{Data frame from \code{stat_cohen()}: first column is
\code{Variable}, remaining columns are Cohen's d per group.}

\item{ref_group}{Character, name of the reference group column used for
sorting. Default \code{NULL} uses the first group column.}

\item{palette}{Colour palette name or character vector of colours.
Default uses \code{pal_lancet}.}

\item{threshold}{Numeric, dashed reference line positions for small effect.
Default \code{c(-0.2, 0.2)}.}

\item{add_line}{Logical, connect dots with lines. Default \code{TRUE}.}

\item{facet}{Logical, facet by group. Default \code{FALSE}.}

\item{base_size}{Base font size. Default 14.}
}
\value{
A ggplot object.
}
\description{
Visualise Cohen's d values as a Cleveland dot plot (lollipop chart).
Variables are ordered by the effect size of a reference group.
Dashed lines at +/-0.2 indicate the small effect threshold.
}
\examples{
# Using iris data
res <- stat_cohen(iris, group = "Species",
                  vars = c("Sepal.Length", "Sepal.Width",
                           "Petal.Length", "Petal.Width"))

# Basic dot plot
plt_cohen(res)

# Sort by a specific group
plt_cohen(res, ref_group = "versicolor")

# With facet per group
plt_cohen(res, facet = TRUE)

# Without connecting lines
plt_cohen(res, add_line = FALSE)

# Custom threshold and palette
plt_cohen(res, threshold = c(-0.5, 0.5), palette = "Paired")

# Simulated multi-group data
df <- data.frame(
  group = factor(sample(c("Control","Treatment","Placebo"), 150, TRUE)),
  age = rnorm(150, 50, 10),
  bmi = rnorm(150, 25, 5),
  score = rnorm(150, 100, 15)
)
res2 <- stat_cohen(df, "group", c("age", "bmi", "score"))
plt_cohen(res2)
plt_cohen(res2, facet = TRUE, palette = "Set2")

}
\seealso{
Other plot formatting: 
\code{\link{fmt_axis}()},
\code{\link{fmt_bg}()},
\code{\link{fmt_boxplot}()},
\code{\link{fmt_com}()},
\code{\link{fmt_expand}()},
\code{\link{fmt_his}()},
\code{\link{fmt_legend}()},
\code{\link{fmt_plot}()},
\code{\link{fmt_point}()},
\code{\link{fmt_ref}()},
\code{\link{fmt_scale}()},
\code{\link{fmt_strip}()},
\code{\link{fmt_tag}()},
\code{\link{plt_dist}()},
\code{\link{plt_radar}()},
\code{\link{plt_sankey}()},
\code{\link{plt_upset}()}
}
\concept{plot formatting}
