% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_plot.R
\name{fmt_legend}
\alias{fmt_legend}
\title{Format legend position and style}
\usage{
fmt_legend(
  plot,
  legend.position = NULL,
  legend.direction = NULL,
  legend_theme = NULL,
  collect = FALSE,
  scale = NULL,
  scale_width = NULL,
  scale_height = NULL,
  ncol = NULL,
  nrow = NULL,
  ...
)
}
\arguments{
\item{plot}{A ggplot, patchwork, or list of ggplot objects.}

\item{legend.position}{Legend position. Accepts:
\itemize{
  \item Character: `"top"`, `"bottom"`, `"left"`, `"right"`, `"none"`.
  \item Shorthand corner codes: `"br"`, `"bl"`, `"tr"`, `"tl"` (inside
    plot corners).
  \item Numeric vector of length 2: `c(x, y)` coordinates (0-1) for
    inside-plot positioning.
}
Default `NULL` (no change).}

\item{legend.direction}{`"horizontal"` or `"vertical"`. Default `NULL`.}

\item{legend_theme}{A ggplot2 theme object for legend styling, e.g.,
[theme_legend1()]. Applied after position/direction settings so it can
override them. Default `NULL` (no extra styling).}

\item{collect}{Logical. If `TRUE` and input has multiple plots,
collect legends into a single shared legend via patchwork. Default `FALSE`.}

\item{scale}{Numeric. Proportionally scale the entire legend.
\code{0.8} = shrink to 80\%, \code{1.2} = enlarge to 120\%.
Adjusts key size, text size, title size, point size, and spacing
together. Default \code{NULL} (no scaling).}

\item{scale_width}{Numeric. Scale legend key width independently.
Default \code{NULL} (no change).}

\item{scale_height}{Numeric. Scale legend key height independently.
Default \code{NULL} (no change).}

\item{ncol}{Number of columns in the legend layout (passed to
[ggplot2::guide_legend()]).}

\item{nrow}{Number of rows in the legend layout (passed to
[ggplot2::guide_legend()]).}

\item{...}{Additional arguments passed to [ggplot2::theme()], e.g.,
`legend.text`, `legend.key.size`, `legend.background`.}
}
\value{
Same type as input.
}
\description{
Adjust legend position, direction, layout, scaling, and optionally
collect legends across a multi-plot patchwork.
}
\examples{
library(ggplot2)
p <- ggplot(iris, aes(Sepal.Length, Sepal.Width, color = Species)) + geom_point()

fmt_legend(p, legend.position = "bottom", legend.direction = "horizontal")
fmt_legend(p, legend.position = "none")
fmt_legend(p, legend.position = "br")
fmt_legend(p, legend.position = c(0.9, 0.2))
fmt_legend(p, legend.position = "br", legend_theme = theme_legend1())

# Scale legend to 80\% of current size
fmt_legend(p, scale = 0.8)

# Scale width and height independently
fmt_legend(p, scale_width = 1.5, scale_height = 0.5)

}
\seealso{
Other plot formatting: 
\code{\link{fmt_axis}()},
\code{\link{fmt_axisText}()},
\code{\link{fmt_axisTile}()},
\code{\link{fmt_bg}()},
\code{\link{fmt_boxplot}()},
\code{\link{fmt_com}()},
\code{\link{fmt_expand}()},
\code{\link{fmt_his}()},
\code{\link{fmt_panel}()},
\code{\link{fmt_plot}()},
\code{\link{fmt_point}()},
\code{\link{fmt_ref}()},
\code{\link{fmt_scale}()},
\code{\link{fmt_strip}()},
\code{\link{fmt_tag}()},
\code{\link{fmt_text}()}
}
\concept{plot formatting}
