% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotDumbbell.R
\name{PlotDumbbell}
\alias{PlotDumbbell}
\title{Dumbbell Chart for Paired Comparisons}
\usage{
PlotDumbbell(
  data,
  x_col = "start",
  xend_col = "end",
  y_col = "category",
  sort_by = "end",
  sort_descending = FALSE,
  top_n = NULL,
  color_x = "#00468BFF",
  color_xend = "#ED0000FF",
  label_x = NULL,
  label_xend = NULL,
  seg_color = "grey60",
  seg_size = 0.8,
  point_size = 3,
  point_shape = 19,
  show_delta = FALSE,
  delta_size = 3,
  delta_digits = 2,
  delta_color = "grey30",
  show_zero_line = TRUE,
  zero_line_color = "grey30",
  zero_line_width = 0.3,
  show_mean_lines = FALSE,
  mean_line_width = 0.3,
  dot_guide = FALSE,
  dot_guide_color = "grey80",
  dot_guide_size = 0.15,
  x_label = NULL,
  y_label = NULL,
  title = NULL,
  legend_pos = "auto",
  legend_theme = NULL,
  theme_use = theme_my,
  inset_plot = NULL,
  inset_type = "boxplot",
  inset_test = "t.test",
  inset_paired_line = TRUE,
  inset_jitter = TRUE,
  inset_theme_use = NULL,
  inset_left = 0.04,
  inset_bottom = 0.68,
  inset_right = 0.35,
  inset_top = 0.94,
  filename = NULL,
  width = 8,
  height = 6,
  dpi = 300,
  bg = "white"
)
}
\arguments{
\item{data}{A data.frame containing category, start, and end columns.}

\item{x_col}{Column name for the start (left) values. Default
\code{"start"}.}

\item{xend_col}{Column name for the end (right) values. Default
\code{"end"}.}

\item{y_col}{Column name for the category labels (Y axis). Default
\code{"category"}.}

\item{sort_by}{How to sort categories: \code{"end"} (by xend_col),
\code{"start"} (by x_col), \code{"delta"} (by difference),
\code{"none"} (original order). Default \code{"end"}.}

\item{sort_descending}{Logical. Sort in descending order? Default
\code{FALSE} (ascending, smallest at bottom).}

\item{top_n}{Integer. Show only top N categories after sorting. Default
\code{NULL} (show all).}

\item{color_x}{Colour for start (x) points. Default \code{"#00468BFF"}.}

\item{color_xend}{Colour for end (xend) points. Default \code{"#ED0000FF"}.}

\item{label_x}{Label for start points (used in legend and auto inset).
Default \code{NULL} (column name).}

\item{label_xend}{Label for end points (used in legend and auto inset).
Default \code{NULL} (column name).}

\item{seg_color}{Segment line colour. Default \code{"grey60"}.}

\item{seg_size}{Segment line width. Default 0.8.}

\item{point_size}{Point size. Default 3.}

\item{point_shape}{Point shape. Default 19 (filled circle).}

\item{show_delta}{Logical. Show delta (difference) labels at the end of
each segment? Default \code{FALSE}.}

\item{delta_size}{Text size for delta labels. Default 3.}

\item{delta_digits}{Number of decimal places for delta labels. Default 2.}

\item{delta_color}{Colour for delta labels. Default \code{"grey30"}.}

\item{show_zero_line}{Logical. Show a vertical line at x = 0? Default
\code{TRUE}.}

\item{zero_line_color}{Colour for zero line. Default \code{"grey30"}.}

\item{zero_line_width}{Width for zero line. Default 0.3.}

\item{show_mean_lines}{Logical. Show vertical dashed lines at the mean of
x and xend values? Default \code{FALSE}.}

\item{mean_line_width}{Width for mean lines. Default 0.3.}

\item{dot_guide}{Logical. Show faint horizontal guide lines from each
point to the Y axis? Default \code{FALSE}.}

\item{dot_guide_color}{Colour for dot guide lines. Default
\code{"grey80"}.}

\item{dot_guide_size}{Width for dot guide lines. Default 0.15.}

\item{x_label}{X axis label. Default \code{NULL} (no label).}

\item{y_label}{Y axis label. Default \code{NULL} (no label).}

\item{title}{Plot title. Default \code{NULL}.}

\item{legend_pos}{Legend position. Accepts:
\itemize{
  \item \code{"none"} — hide legend.
  \item \code{"auto"} — inside bottom-right (default).
  \item \code{"br"}, \code{"bl"}, \code{"tr"}, \code{"tl"} — inside
    corners.
  \item \code{"bottom"}, \code{"top"}, \code{"left"}, \code{"right"} —
    outside (standard ggplot2 positions).
}}

\item{legend_theme}{A ggplot2 theme object for legend styling, e.g.,
\code{theme_legend1()}. Default \code{NULL} (no extra legend styling).}

\item{theme_use}{Theme function, string, or theme object. Default
\code{theme_my}. Controls axis text/title/line styling.}

\item{inset_plot}{Inset plot specification. One of:
\itemize{
  \item \code{NULL} — no inset (default).
  \item \code{"auto"} — auto-generate a paired boxplot from the dumbbell
    data, with paired lines, jitter points, and a significance bracket.
  \item A \code{ggplot} object — embed a custom inset plot.
}}

\item{inset_type}{Type of auto-generated inset: \code{"boxplot"} (default)
or \code{"violin"}. Only used when \code{inset_plot = "auto"}.}

\item{inset_test}{Paired test for auto inset significance bracket:
\code{"t.test"}, \code{"wilcox"}, or \code{"none"}. Default
\code{"t.test"}. Only used when \code{inset_plot = "auto"}.}

\item{inset_paired_line}{Logical. Show paired connecting lines in auto
inset? Default \code{TRUE}.}

\item{inset_jitter}{Logical. Show jitter points in auto inset? Default
\code{TRUE}.}

\item{inset_theme_use}{Theme for auto inset. Default \code{NULL} (uses
the same \code{theme_use} as the main plot with compact styling).}

\item{inset_left, inset_bottom, inset_right, inset_top}{Inset boundaries
(patchwork panel coordinates). Default 0.04, 0.68, 0.35, 0.94.}

\item{filename}{Output file path. Default \code{NULL} (no save, print
only).}

\item{width, height}{Output dimensions in inches. Default 8 / 6.}

\item{dpi}{Output resolution. Default 300.}

\item{bg}{Output background colour. Default \code{"white"}.}
}
\value{
A ggplot (or patchwork) object (returned invisibly).
}
\description{
Create a horizontal dumbbell chart showing paired values (e.g., before/after,
control/treatment) for each category. Supports auto-sorting, mean reference
lines, delta labels, and an optional inset plot via patchwork.
}
\examples{
\dontrun{
df <- data.frame(
  cell_type = paste0("Type", 1:10),
  sil_ref   = runif(10, -0.2, 0.5),
  sil_emb   = runif(10, 0, 0.8)
)
# Basic dumbbell
PlotDumbbell(df, x_col = "sil_ref", xend_col = "sil_emb",
             y_col = "cell_type", label_x = "PCA", label_xend = "scMMR")

# Auto inset paired boxplot
PlotDumbbell(df, x_col = "sil_ref", xend_col = "sil_emb",
             y_col = "cell_type", label_x = "PCA", label_xend = "scMMR",
             inset_plot = "auto", inset_test = "t.test")

# Custom inset
my_inset <- ggplot2::ggplot(df, ggplot2::aes(sil_ref, sil_emb)) +
  ggplot2::geom_point()
PlotDumbbell(df, x_col = "sil_ref", xend_col = "sil_emb",
             y_col = "cell_type", inset_plot = my_inset)
}

}
