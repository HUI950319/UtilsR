% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_cat.R
\name{fct_cat}
\alias{fct_cat}
\title{Unified Factor Manipulation}
\usage{
fct_cat(
  x,
  ...,
  reverse = FALSE,
  binary_ref = NULL,
  groups = NULL,
  new_labels = NULL,
  combine = NULL,
  sep = " & "
)
}
\arguments{
\item{x}{A factor or character vector. For \code{combine} mode this is
optional -- if missing, all columns listed in \code{combine} are used.}

\item{...}{For recode: named args (new = old). For reorder: level names
in desired order (unnamed strings). Must be ALL named or ALL unnamed.}

\item{reverse}{Logical. Reverse all levels? Default \code{FALSE}.}

\item{binary_ref}{Integer index(es) of reference level(s). Others
collapse to \code{"Oth"}.}

\item{groups}{Named list of integer index vectors for custom grouping,
e.g. \code{list(early = 1:2, late = 3:4)}.}

\item{new_labels}{Character vector to rename levels (same length as levels).}

\item{combine}{Character vector of column names to combine. Works inside
\code{mutate()}. If \code{x} is provided, it is combined with the
listed columns. If \code{x} is missing, all listed columns are combined.}

\item{sep}{Separator for combine. Default \code{" & "}.}
}
\value{
A factor.
}
\description{
One function for all factor operations. Works on vectors (usable in
\code{mutate()}) and auto-detects the action from arguments.
}
\examples{
x <- factor(letters[1:6])

# Recode (named ...)
fct_cat(x, AB = c("a","b"), CD = c("c","d"))
fct_cat(x, g1 = 1:3, g2 = 4:6)

# Reorder (unnamed ...)
fct_cat(factor(letters[1:4]), "c", "b")

# Reverse
fct_cat(factor(letters[1:4]), reverse = TRUE)

# Binary
fct_cat(factor(c("I","II","III","IV")), binary_ref = 1)

# Group
fct_cat(factor(c("I","II","III","IV")), groups = list(early = 1:2, late = 3:4))

# Relabel
fct_cat(factor(c("I","II","III","IV")), new_labels = c("One","Two","Three","Four"))

# Combine in mutate():
\dontrun{
df \%>\% mutate(grp = fct_cat(sex, combine = "age"))
df \%>\% mutate(grp = fct_cat(sex, combine = c("age", "race")))
df \%>\% mutate(grp = fct_cat(combine = c("sex", "age")))
}

}
\seealso{
Other factor tools: 
\code{\link{fct_num}()}
}
\concept{factor tools}
