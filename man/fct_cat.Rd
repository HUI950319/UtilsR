% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_cat.R
\name{fct_cat}
\alias{fct_cat}
\title{Unified Factor Manipulation}
\usage{
fct_cat(
  x,
  ...,
  reverse = FALSE,
  binary_ref = NULL,
  groups = NULL,
  new_labels = NULL
)
}
\arguments{
\item{x}{A factor or character vector.}

\item{...}{For recode: named args (new = old). For reorder: level names
in desired order (unnamed strings). Must be ALL named or ALL unnamed.}

\item{reverse}{Logical. Reverse all levels? Default \code{FALSE}.}

\item{binary_ref}{Integer index(es) of reference level(s). Others
collapse to \code{"Oth"}.}

\item{groups}{Named list of integer index vectors for custom grouping,
e.g. \code{list(early = 1:2, late = 3:4)}.}

\item{new_labels}{Character vector to rename levels (same length as levels).}
}
\value{
A factor.
}
\description{
One function for all single-vector factor operations. Auto-detects the
action from arguments. For combining multiple columns into one factor,
see [fct_to_combine()].
}
\examples{
x <- factor(letters[1:6])

# Recode (named ...)
fct_cat(x, AB = c("a","b"), CD = c("c","d"))
fct_cat(x, g1 = 1:3, g2 = 4:6)

# Reorder (unnamed ...)
fct_cat(factor(letters[1:4]), "c", "b")

# Reverse
fct_cat(factor(letters[1:4]), reverse = TRUE)

# Binary
fct_cat(factor(c("I","II","III","IV")), binary_ref = 1)

# Group
fct_cat(factor(c("I","II","III","IV")), groups = list(early = 1:2, late = 3:4))

# Relabel
fct_cat(factor(c("I","II","III","IV")), new_labels = c("One","Two","Three","Four"))

# For combining multiple columns into one factor, use [fct_to_combine()]:
\dontrun{
df \%>\% mutate(grp = fct_to_combine(pick(sex, age)))
fct_to_combine(df, sex, age)
}

}
\seealso{
Other factor tools: 
\code{\link{fct_num}()},
\code{\link{fct_to_combine}()}
}
\concept{factor tools}
