% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plt_con.R
\name{plt_con}
\alias{plt_con}
\title{Unified Continuous Variable Plot}
\usage{
plt_con(
  data,
  stat.by,
  group.by = NULL,
  split.by = NULL,
  bg.by = NULL,
  type = c("violin", "box", "bar", "dot"),
  fill.by = c("group", "feature"),
  palette = NULL,
  alpha = 0.8,
  add_box = FALSE,
  box_color = "black",
  box_width = 0.1,
  add_point = FALSE,
  pt.color = "grey30",
  pt.size = NULL,
  pt.alpha = 1,
  jitter.width = 0.4,
  jitter.height = 0.1,
  add_trend = FALSE,
  trend_color = "black",
  trend_linewidth = 1,
  trend_ptsize = 2,
  comparisons = NULL,
  ref_group = NULL,
  pairwise_method = "wilcox.test",
  multiplegroup_comparisons = FALSE,
  multiple_method = "kruskal.test",
  sig_label = c("p.signif", "p.format"),
  sig_labelsize = 3.5,
  same.y.lims = FALSE,
  y.min = NULL,
  y.max = NULL,
  y.nbreaks = 5,
  sort = FALSE,
  stack = FALSE,
  flip = FALSE,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  legend.position = "right",
  legend.direction = "vertical",
  aspect.ratio = NULL,
  base_size = 14,
  bg_palette = NULL,
  bg_alpha = 0.15,
  facet_nrow = NULL,
  facet_ncol = NULL,
  combine = TRUE,
  force = FALSE,
  seed = 11
)
}
\arguments{
\item{data}{A data frame.}

\item{stat.by}{Character vector. Column name(s) of numeric variables to
plot.}

\item{group.by}{Character. Column name for the x-axis grouping variable.
Default \code{NULL} (all data in one group).}

\item{split.by}{Character. Optional splitting variable. Splits data,
creates one plot per level, and combines with \pkg{patchwork}.}

\item{bg.by}{Character. Column name for background colour bands.
Must be a superset of \code{group.by}. Default \code{NULL}.}

\item{type}{Plot type: \code{"violin"} (default), \code{"box"},
\code{"bar"}, or \code{"dot"}.}

\item{fill.by}{What variable to map to fill colour:
\code{"group"} (default) colours by group.by levels;
\code{"feature"} colours by stat.by name.}

\item{palette}{Colour palette. One of:
\itemize{
  \item \code{NULL} (default): uses \code{pal_lancet}.
  \item A single string matching a name in \code{palette_list}: uses
    \code{pal_get()}.
  \item A character vector of colours: used directly.
}}

\item{alpha}{Numeric 0--1. Fill transparency. Default 0.8.}

\item{add_box}{Logical. Overlay boxplot on violin? Default \code{FALSE}.}

\item{box_color}{Character. Box overlay colour. Default \code{"black"}.}

\item{box_width}{Numeric. Box overlay width. Default 0.1.}

\item{add_point}{Logical. Overlay jittered points? Default \code{FALSE}.}

\item{pt.color}{Character. Point colour. Default \code{"grey30"}.}

\item{pt.size}{Numeric. Point size. Default \code{NULL} (auto).}

\item{pt.alpha}{Numeric. Point transparency. Default 1.}

\item{jitter.width}{Numeric. Jitter width. Default 0.4.}

\item{jitter.height}{Numeric. Jitter height. Default 0.1.}

\item{add_trend}{Logical. Overlay trend line connecting medians/means?
Default \code{FALSE}.}

\item{trend_color}{Character. Trend line colour. Default \code{"black"}.}

\item{trend_linewidth}{Numeric. Trend line width. Default 1.}

\item{trend_ptsize}{Numeric. Trend point size. Default 2.}

\item{comparisons}{A list of length-2 character vectors for pairwise tests.
Default \code{NULL}.}

\item{ref_group}{Character. Reference group for comparisons.
Default \code{NULL}.}

\item{pairwise_method}{Character. Pairwise test method.
Default \code{"wilcox.test"}.}

\item{multiplegroup_comparisons}{Logical. Add global comparison?
Default \code{FALSE}.}

\item{multiple_method}{Character. Global test method.
Default \code{"kruskal.test"}.}

\item{sig_label}{Label type: \code{"p.signif"} or \code{"p.format"}.}

\item{sig_labelsize}{Numeric. Label text size. Default 3.5.}

\item{same.y.lims}{Logical. Use same y limits across features?
Default \code{FALSE}.}

\item{y.min}{Numeric or character. Minimum y-axis limit.
Character \code{"qN"} uses the Nth percentile. Default \code{NULL}.}

\item{y.max}{Numeric or character. Maximum y-axis limit.
Character \code{"qN"} uses the Nth percentile. Default \code{NULL}.}

\item{y.nbreaks}{Integer. Number of y-axis breaks. Default 5.}

\item{sort}{Logical or character. Sort groups by median?
\code{TRUE} or \code{"decreasing"} for descending,
\code{"increasing"} for ascending. Default \code{FALSE}.}

\item{stack}{Logical. Stack features vertically using facet?
Default \code{FALSE}.}

\item{flip}{Logical. Flip coordinates? Default \code{FALSE}.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{subtitle}{Character. Plot subtitle. Default \code{NULL}.}

\item{xlab}{Character. X-axis label. Default \code{NULL}.}

\item{ylab}{Character. Y-axis label. Default \code{NULL}.}

\item{legend.position}{Legend position. Default \code{"right"}.}

\item{legend.direction}{Legend direction. Default \code{"vertical"}.}

\item{aspect.ratio}{Numeric. Panel aspect ratio. Default \code{NULL}.}

\item{base_size}{Numeric. Base font size for \code{theme_my()}.
Default 14.}

\item{bg_palette}{Character vector. Background band colours.
Default \code{NULL}.}

\item{bg_alpha}{Numeric. Background band transparency. Default 0.15.}

\item{facet_nrow}{Integer. Rows when combining panels.
Default \code{NULL}.}

\item{facet_ncol}{Integer. Columns when combining panels.
Default \code{NULL}.}

\item{combine}{Logical. \code{TRUE} returns single patchwork,
\code{FALSE} returns list. Default \code{TRUE}.}

\item{force}{Logical. Override >100 level safety? Default \code{FALSE}.}

\item{seed}{Integer. Random seed for jitter. Default 11.}
}
\value{
A ggplot/patchwork object (if \code{combine = TRUE}) or a named
  list of ggplots.
}
\description{
A single entry point for continuous/numeric variable plotting with
violin, box, bar (mean +/- SD), and dot plot types. Supports statistical
comparisons via \pkg{ggpubr}, overlay layers (boxplot, jitter points,
trend lines), and the same split/group/bg patterns as
\code{\link{plt_cat}}.
}
\examples{
set.seed(1)
grp <- factor(sample(c("A", "B", "C"), 200, TRUE))
df <- data.frame(
  value1 = rnorm(200),
  value2 = rnorm(200, mean = 2),
  group  = grp,
  batch  = factor(sample(c("B1", "B2"), 200, TRUE)),
  region = factor(ifelse(grp == "C", "R2", "R1"))
)

# Basic violin plot
plt_con(df, "value1", "group")

\donttest{
# ===== Basic types =====
plt_con(df, "value1", "group", type = "box")
plt_con(df, "value1", "group", type = "bar")
plt_con(df, "value1", "group", type = "dot")

# ===== Overlays =====
plt_con(df, "value1", "group", add_box = TRUE)
plt_con(df, "value1", "group", type = "box", add_point = TRUE)
plt_con(df, "value1", "group", type = "bar", add_trend = TRUE)
plt_con(df, "value1", "group", add_point = TRUE, add_box = TRUE)

# ===== Multiple features =====
plt_con(df, c("value1", "value2"), "group")
plt_con(df, c("value1", "value2"), "group", stack = TRUE)
plt_con(df, c("value1", "value2"), "group", fill.by = "feature")

# ===== Statistical comparisons =====
plt_con(df, "value1", "group",
        comparisons = list(c("A", "B"), c("A", "C")))
plt_con(df, "value1", "group",
        multiplegroup_comparisons = TRUE, sig_label = "p.format")

# ===== Layout =====
plt_con(df, "value1", "group", flip = TRUE)
plt_con(df, "value1", "group", split.by = "batch")
plt_con(df, "value1", "group", bg.by = "region")
plt_con(df, "value1", "group", sort = TRUE)

# ===== Y-axis control =====
plt_con(df, c("value1", "value2"), "group",
        same.y.lims = TRUE, y.max = 4)
}

}
\seealso{
Other plot: 
\code{\link{plt_cat}()},
\code{\link{plt_cohen}()},
\code{\link{plt_dist}()},
\code{\link{plt_radar}()},
\code{\link{plt_sankey}()},
\code{\link{plt_upset}()}
}
\concept{plot}
