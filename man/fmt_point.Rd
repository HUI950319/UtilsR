% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_point.R
\name{fmt_point}
\alias{fmt_point}
\title{Add Data Points to a Plot}
\usage{
fmt_point(
  plot,
  type = c("point", "jitter", "beeswarm"),
  data = NULL,
  shape = 19,
  size = 1,
  white_border = FALSE,
  dodge.width = 0.8,
  jitter.width = 0.2,
  jitter.height = 0,
  beeswarm.args = list(cex = 3, corral = "wrap", corral.width = 0.5),
  rasterize = FALSE,
  rasterize.dpi = 300,
  ...
)
}
\arguments{
\item{plot}{A ggplot, patchwork, or list of ggplot objects.}

\item{type}{Point layout type: \code{"point"} (default), \code{"jitter"},
or \code{"beeswarm"}.}

\item{data}{Data for the point layer. Default \code{NULL} inherits from plot.
Can be a data.frame or a function/formula applied to the plot data
(e.g. \code{~ subset(.x, value > 10)}).}

\item{shape}{Point shape. Default 19 (solid circle).}

\item{size}{Point size. Default 1.}

\item{white_border}{Logical. If \code{TRUE}, converts shapes to fillable
versions (21-24) with white stroke. Default \code{FALSE}.}

\item{dodge.width}{Dodge width for grouped data. Default 0.8.
Set to 0 to disable dodging.}

\item{jitter.width}{Horizontal jitter. Only used when \code{type = "jitter"}.
Default 0.2.}

\item{jitter.height}{Vertical jitter. Default 0.}

\item{beeswarm.args}{Named list of arguments for beeswarm layout.
Only used when \code{type = "beeswarm"}.
Defaults: \code{list(cex = 3, corral = "wrap", corral.width = 0.5)}.}

\item{rasterize}{Logical. If \code{TRUE}, rasterizes the point layer
via \code{ggrastr::rasterise} for large datasets. Default \code{FALSE}.}

\item{rasterize.dpi}{Integer, DPI for rasterization. Default 300.}

\item{...}{Additional arguments passed to \code{geom_point} or
\code{geom_beeswarm}.}
}
\value{
Same type as input (ggplot, patchwork, or list).
}
\description{
Unified function to overlay data points on ggplot objects. Supports
plain points, jittered points, and beeswarm layout. Automatically
detects group aesthetics to apply dodge positioning.
}
\examples{
library(ggplot2)
p <- ggplot(iris, aes(Species, Sepal.Length)) + geom_boxplot()

# Plain points
fmt_point(p, color = "steelblue", alpha = 0.5)

# Jittered points
fmt_point(p, type = "jitter", color = "red", alpha = 0.5)

# Beeswarm
fmt_point(p, type = "beeswarm", color = "darkgreen", size = 1.2)

# White border effect
fmt_point(p, type = "jitter", fill = "purple",
          white_border = TRUE, size = 2)

# Grouped data (auto-dodge)
p2 <- ggplot(iris, aes(Species, Sepal.Length, color = Species)) +
  geom_boxplot()
fmt_point(p2, type = "jitter", alpha = 0.5)

}
\seealso{
Other plot formatting: 
\code{\link{fmt_axis}()},
\code{\link{fmt_bg}()},
\code{\link{fmt_boxplot}()},
\code{\link{fmt_com}()},
\code{\link{fmt_expand}()},
\code{\link{fmt_his}()},
\code{\link{fmt_legend}()},
\code{\link{fmt_plot}()},
\code{\link{fmt_ref}()},
\code{\link{fmt_scale}()},
\code{\link{fmt_strip}()},
\code{\link{fmt_tag}()}
}
\concept{plot formatting}
